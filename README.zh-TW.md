# ⚡ ChipForAll (C4O) - 開源晶片設計懶人包

![CI Status](https://github.com/anlit75/ChipForAll/actions/workflows/verify.yml/badge.svg)
![License](https://img.shields.io/github/license/anlit75/ChipForAll)
![Docker](https://img.shields.io/badge/docker-ready-blue)

> **專為初學者打造的零配置 (Zero-Config) 晶片設計環境。**
> 專注於你的 Verilog 邏輯，別再為了安裝 EDA 工具浪費生命。

---

## 🚀 快速開始 (Quick Start)

**三步搞定環境配置：**

1.  **建立專案**：點擊右上角的 **[Use this template](https://github.com/anlit75/ChipForAll/generate)** 按鈕，複製一份到你的帳號。
2.  **啟動環境**：在你的新專案頁面，點擊綠色 **Code** 按鈕 -> **Codespaces** -> **Create codespace**。
3.  **執行範例**：
    ```bash
    make test
    ```
    *你應該會看到測試通過的訊息，並且在 `build/` 資料夾中生成了波形檔。*

---

## 🔋 特色功能 (Features)

這個模板提供了一套開箱即用的 EDA (電子設計自動化) 環境：

* **🐳 預先配置的 Docker 環境**：不需要手動安裝 Yosys, Verilator 或 Icarus Verilog。VS Code 會自動拉取 `ghcr.io/anlit75/c4o-core` 映像檔。
* **🛠️ 統一的 Makefile**：無論你是用 Linux, Mac 還是 Windows (WSL)，指令都是一樣的。Makefile 會自動判斷是否需要透過 Docker 執行。
* **✅ CI/CD 自動化測試**：GitHub Actions 已經設定好了。每次 Push 程式碼，雲端會自動幫你跑語法檢查、模擬與合成測試。
* **💻 VS Code 最佳化**：內建 Verilog 語法高亮與開發工具設定。

---

## 📂 專案結構

```text
.
├── src/                # 你的 RTL 設計檔 (Verilog)
│   └── blinky.v        # 範例：一個簡單的 LED 閃爍電路
├── test/               # 你的 Testbench (測試平台)
│   └── tb_blinky.v     # 範例：驗證 blinky 模組的測試檔
├── build/              # 產出物存放區 (波形檔 .vcd, 合成網表 .json)
├── Makefile            # 智慧型腳本 (自動處理 Docker/Local 執行環境)
└── .github/            # CI 自動化流程設定
```

---

## 🛠️ 常用指令

本專案使用智慧型 Makefile。如果你本機有安裝工具，它會直接執行；如果沒有，它會自動呼叫 Docker 容器執行。

### 1. 語法檢查 (Lint)
使用 `verilator --lint-only` 快速檢查 Verilog 語法錯誤。
```bash
make lint
```

### 2. 執行模擬 (Test)
使用 `iverilog` 編譯並執行模擬。生成的波形檔 (`.vcd`) 會儲存在 `build/` 目錄下。
```bash
make test
```

### 3. 電路合成 (Synthesize)
使用 `yosys` 將你的 Verilog 轉換為邏輯閘層級的網表 (Gate-level Netlist)。
```bash
make synth
```

### 4. 清理環境
刪除 `build/` 目錄下的暫存檔。
```bash
make clean
```

## 🏗️ 實體設計 (ASIC GDSII 流程)

使用 **OpenLane** 與 **SkyWater 130nm PDK** 將你的 Verilog 轉換為可生產的晶片佈局。

### 1. 安裝 PDK (僅首次需要)
透過 `volare` 下載並安裝 Sky130 PDK (約需下載 3GB 數據，請耐心等待)。
```bash
make pdk
```

### 2. 產生 GDSII 佈局檔
在 Docker 容器中執行完整的 OpenLane 流程 (合成 -> 佈局 -> 繞線 -> 簽核)。
```bash
make gds
```
* 產出檔案: `build/blinky.gds`
* 檢視工具: 推薦使用開源的 [KLayout](https://www.klayout.de/) 開啟 GDS 檔，親眼看看你設計的晶片電路結構！

---

## 🤝 參與貢獻

歡迎提交 Issue 或 Pull Request！讓我們一起降低晶片設計的門檻。

Maintained by [anlit75](https://github.com/anlit75).
